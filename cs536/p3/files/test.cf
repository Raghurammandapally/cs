int x;
int y;
bool z;
void xyz;
struct c{int l1;};
struct test {

  struct c{int l1;};
int x;
int y;
bool z;
} ;
int max(int num1, int num2) {}
int max(int x, bool y, void z) 
{
  int result;
  max();
  return;
  max(1,2,3);
  max("hello world",true,false, (3), max(65, "yah"), x.y.zba, a.c, c );
  max(1+ 2, 3 - 4, 4 * 5 - 3, 7 + 8 * 2, !4);
  max(-2, 1 < 2, 3 >= 4, 5 == 3, 1 < 2);//< 3);
  a = b;
  a++; b--;
  cin >> x.yz;
  cout << 5 * 32;
  if(hello==world){println("yah");}
  if(hello==world){println("yah");}else{doSomethingDifferent();}
  while(dog){cat();}
  return(a == b*c);
}

